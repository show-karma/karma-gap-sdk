{
  "name": "karma-gap-sdk",
  "version": "0.0.1",
  "description": "Simple and easy interface between EAS and Karma GAP.",
  "main": "dist/index.js",
  "author": "KarmaHQ",
  "license": "MIT",
  "private": false,
  "scripts": {
    "prepublishOnly": "npx tsc && git add ./.dist && git commit -m 'chore: update dist' && npm version patch && git push",
    "deploy": "npx ts-node ./core/scripts/deploy.ts"
  },
  "dependencies": {
    "@ethereum-attestation-service/eas-sdk": "^1.1.0-beta.2",
    "axios": "^1.4.0",
    "ethers": "5.7",
    "simple-ts-job-runner": "^1.0.12"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "eslint": "^8.47.0",
    "husky": "^8.0.3",
    "prettier": "^3.0.1",
    "typescript": "^5.1.6"
  }
}
